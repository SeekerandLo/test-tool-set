import React from 'react';

const Index = (props, states) => {
  const { history } = props;
  return (
    <div>
      <div style={{ fontSize: 30, fontWeight: 600 }}>TTS</div>
    </div>
  );
};

export default Index;
